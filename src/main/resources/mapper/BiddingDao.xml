<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >


<mapper namespace="com.stonedt.spider.dao.BiddingDao">
	<select id="getInformationtype" resultType="map">
		SELECT
		informationtype,
		count( 1 ) AS informationtypeCount
		FROM
		invitation.invitation_list
		GROUP BY
		informationtype
	</select>
</mapper>